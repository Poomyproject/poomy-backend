package com.poomy.mainserver.user.repository;

import com.poomy.mainserver.user.entity.UserAtmosphere;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserAtmosphereRepository extends JpaRepository<UserAtmosphere, Long> {
}
